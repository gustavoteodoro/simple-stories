import React, { Component } from 'react';

import Logo from '../../elements/Logo';

import {
    HomeContainer,
    HomeContent,
} from './styles';

class Home extends Component {
  render() {
    return (
      <HomeContainer>
        <HomeContent>
        </HomeContent>
      </HomeContainer>
    );
  }
}

export default Home;
